// import { addBusketData } from "./add-busket-data";

// export const saveProductInBusket = (requestServer, newBusketData) => (dispatch) =>{
//   requestServer("saveProductInBusket", newBusketData)
//     dispatch(addBusketData(newBusketData));
//   };